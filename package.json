{
  "name": "selector",
  "version": "0.3.8",
  "private": true,
  "scripts": {
    "build": "node scripts/build.js",
    "check": "npm run build && cat dist/assets/editor/*.txt | node --check"
  }
}
